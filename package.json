{
  "name": "esbuild-reproduce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --sourcemap --target=chrome90,safari14 --outfile=public/app.js",
    "start": "esbuild index.js --servedir=public --outdir=public --bundle"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.11.22",
    "postcss-value-parser": "^4.1.0"
  }
}
